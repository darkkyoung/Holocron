import { sqliteTable,text,index } from 'drizzle-orm/sqlite-core';
export const articles=sqliteTable('articles',{id:text('id').primaryKey(),topic:text('topic').notNull(),topicOverride:text('topic_override'),title:text('title').notNull(),summary:text('summary').notNull(),image:text('image').notNull(),url:text('url').notNull().unique(),source:text('source').notNull(),published:text('published').notNull(),category:text('category').notNull(),status:text('status').notNull().default('published'),statusOverride:text('status_override'),reason:text('reason').notNull().default(''),franchise:text('franchise').notNull().default('star-wars')},t=>[index('idx_articles_status_topic').on(t.status,t.topic),index('idx_articles_status_published').on(t.status,t.published)]);
export const settings=sqliteTable('settings',{key:text('key').primaryKey(),value:text('value').notNull()});
