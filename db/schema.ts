import { sqliteTable,text,index } from 'drizzle-orm/sqlite-core';
export const articles=sqliteTable('articles',{id:text('id').primaryKey(),topic:text('topic').notNull(),title:text('title').notNull(),summary:text('summary').notNull(),image:text('image').notNull(),url:text('url').notNull().unique(),source:text('source').notNull(),published:text('published').notNull(),category:text('category').notNull(),status:text('status').notNull().default('published'),reason:text('reason').notNull().default(''),franchise:text('franchise').notNull().default('star-wars')},t=>[index('idx_articles_status_topic').on(t.status,t.topic)]);
export const settings=sqliteTable('settings',{key:text('key').primaryKey(),value:text('value').notNull()});
